Al igual de como se mencionó anteriormente, en este mismo directorio (directorio de nombre "Codigos"), se encuentra un archivo de nombre "CODIGOS_A_EJECUTAR.ipynb", este archivo es de formato jupyter (es un jupyter notebook), y este contiene los codigos necesarios para replicar los resultados obtenidos.

**Para poder replicar los resultados obtenidos**, al igual de como se mencionó anteriormente (en el readme inicial, readme de nombre "README.md"), para poder ejecutar el codigo, hay que descargar el archivo "CODIGOS_A_EJECUTAR.ipynb" y el archivo "G(t)_T(t)_FdT.csv", luego se deben agregar ambos archivos en una misma carpeta (la persona que quiera ejecutar el código deberá crear una carpeta externa o usar alguna que ya exista). Además de lo anterior, la persona deberá verificar tener todos los requisitos ya descargados (requisitos de versión de python, requisito de las librerias usadas, requisitos de que sean compatibles las librerias a usar con la versión de python).

Se propone una segunda alternativa para poder replicar los resultados presentados. **Esta segunda opción es la que nosotros recomendamos**, ya que no hay nada que instalar. Lo que se debe hacer es descargar los archivos "CODIGOS_A_EJECUTAR.ipynb" y "G(t)_T(t)_FdT.csv" (este archivo está en el directorio de nombre "Datos"). Luego de tener los dos archivos descargados hay que ir a google colab y subir el archivo "CODIGOS_A_EJECUTAR.ipynb", con esto cargaremos el archivo que contiene todos los códigos necesarios. Luego, hay que subir el archivo con los datos a usar, archivo de nombre "G(t)_T(t)_FdT.csv" (no está de más mencionar que este archivo se debe subir en la sección de archivos dentro de google colab). Con lo anterior ya hecho, ahora se pueden ejecutar los códigos sin problemas. (recomendamos esta forma de ejecutar los códigos ya que es más amigable para las personas que no están familiarizadas con la programación)
